{
  "name": "folk-job",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.7.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "deploy": "pnpm deploy:ocr && pnpm deploy:api && pnpm deploy:web",
    "dev": "pnpm --parallel --filter @folk-job/api --filter @folk-job/web run dev",
    "dev:api": "pnpm --filter @folk-job/api run dev",
    "dev:web": "pnpm --filter @folk-job/web run dev",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm --filter @folk-job/api run test && pnpm --filter @folk-job/ocr run test",
    "cf:typegen": "pnpm --filter @folk-job/ocr run cf:typegen && pnpm --filter @folk-job/api run cf:typegen && pnpm --filter @folk-job/web run cf:typegen",
    "deploy:api": "pnpm --filter @folk-job/api run deploy",
    "deploy:ocr": "pnpm --filter @folk-job/ocr run deploy",
    "deploy:web": "pnpm --filter @folk-job/web run deploy"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
